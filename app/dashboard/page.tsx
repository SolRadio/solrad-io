import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "SOLRAD — Dashboard",
  robots: { index: false, follow: false },
}

export default function DashboardPage() {
  return null
}
